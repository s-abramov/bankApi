package ru.sber.bankapi.service;

import org.junit.Test;

import java.io.IOException;
import java.net.HttpURLConnection;
import java.net.URL;

public class RestServerTest {
/*
    private static final String HOSTNAME = "http://127.0.0.1:8081";
    private static final String GET_BALANCE = "/getbalance";

    @Test
    public void test() throws IOException {
        new RestServer().startHttpServer();
        URL link = new URL(HOSTNAME + GET_BALANCE);
        HttpURLConnection connection = (HttpURLConnection)link.openConnection();
        connection.getInputStream();
        connection.getOutputStream();

        //connection.disconnect();

    }*/
}
